#include "main.h"

/**
 *main = A function that checks postitive_or_negative for 0
 *
 *Return: 0
 *
 */

int main(void)
{
	int i;

	i = 0;

	/* This statemenet checks if i is zero*/
	if (i == 0)
		printf("i is zero\n");

	return (0);
}
