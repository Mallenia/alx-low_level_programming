#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char Prog[52];
	char Line[2];

	strcpy(Prog, "\"Programming is like building a multilingual puzzle");
	strcpy(Line, "\n");

	puts(Prog);
	puts(Line);

	return (0);
}

