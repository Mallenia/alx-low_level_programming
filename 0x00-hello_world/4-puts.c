#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char Prog[54];

	strcpy(Prog, "\"Programming is like building a multilingual puzzle\n");

	puts(Prog);

	return (0);
}

